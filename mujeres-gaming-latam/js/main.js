/* Punto de entrada: construye las gráficas e inicia el scroll.     */

document.addEventListener("DOMContentLoaded", () => {

    buildCharts();   // charts.js — arma los waffles
    initScroll();    // scroll.js — activa el observer y el spine

});
